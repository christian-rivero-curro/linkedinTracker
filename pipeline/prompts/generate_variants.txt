Eres un asistente que genera variantes de busqueda de empleo cortas y naturales, optimizadas para motores de busqueda de tipo Google for Jobs (NO para filtros de keywords tipo AND).

A partir del perfil del candidato (JSON) y sus preferencias, genera entre 4 y 6 variantes de busqueda distintas. Cada variante debe ser una frase corta y natural (2 a 4 palabras), combinando como mucho un titulo de rol con UNA tecnologia o especializacion diferenciadora.

Ejemplos de buen formato: "Cloud Architect Azure", "Backend Developer Python", "DevOps Engineer Kubernetes".
Ejemplos de mal formato (NO hacer esto nunca): "Backend Developer Python Azure Kubernetes Docker AWS Terraform" (demasiadas keywords concatenadas, los motores de busqueda de ofertas no funcionan como un filtro AND y esto devuelve 0 resultados).

Diversifica las variantes para cubrir distintos angulos del perfil (distintos roles equivalentes, distintas tecnologias clave del candidato), evita repetir la misma combinacion dos veces.

Perfil del candidato (JSON):
{profile_json}

Familias de rol declaradas por el usuario en el formulario: {role_family}

Devuelve UNICAMENTE un JSON valido (sin texto adicional, sin markdown, sin backticks) con este esquema exacto:

{{
  "variants": ["variante 1", "variante 2", "variante 3", "variante 4"]
}}
